# Paper TODO — deferred items (do not change current analysis)

Items from an internal methods review, deferred to the paper stage so the
current analysis stays frozen.

1. **Human validation sample** — draw a random sample of judged responses and
   have humans label them against the rubric, to report judge–human agreement
   alongside the existing cross-judge agreement numbers.

2. **Native-speaker zh review** — native-speaker review of the `zh` prompt
   translations (and ideally a sample of `zh` responses) before any strong
   cross-language claims. Already flagged in `results/results.md` Limits.

3. **Model-level demotion** — demote developer-country grouping claims to
   model-level claims throughout; with `n = 2` models per group, the model is
   the defensible unit of analysis. Grouping stays descriptive only.

4. **Age-removed variant** — rerun with age disclosures removed from the
   prompts to isolate how much minor-status disclosure drives model behavior.
   Already flagged in `results/results.md` Limits.
